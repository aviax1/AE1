method: random
metric:
  goal: minimize
  name: ""
parameters:
  Latent_size:
    distribution: int_uniform
    max: 256
    min: 8
  batch_size:
    distribution: int_uniform
    max: 256
    min: 50
  channels_two:
    distribution: int_uniform
    max: 64
    min: 16
  dropout:
    distribution: uniform
    max: 1
    min: 0.25
  hidden_size:
    distribution: int_uniform
    max: 256
    min: 8
  learning_rate:
    distribution: uniform
    max: 0.01
    min: 0.0001
  num_epochs:
    distribution: int_uniform
    max: 200
    min: 20
program: train.py
